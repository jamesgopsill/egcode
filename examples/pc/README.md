# PC demo

This folder contains a demonstration of how `egcode` can be used in a `std` environment. Simply clone the repo, `cd` into the directory and run:

```
cargo run
```

The demonstration encrypts and decrypts gcode using the password + device_key method.
