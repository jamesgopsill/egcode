# egcode

A rust crate and standard encrypting, signining, verifying and descrypting gcode. The repository features two crates:

- `egcode` the `no_std` `async` `streaming` encryption/decryption crate for gcode.
- `webapp` the [demonstrator website]() where you can see egcode in action.
